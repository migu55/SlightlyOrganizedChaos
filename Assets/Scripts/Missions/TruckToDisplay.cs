using UnityEngine;

public class TruckToDisplay : MonoBehaviour
{
    public GameObject missionDisplay;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
